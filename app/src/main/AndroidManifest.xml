<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="quackathon.avalon">

    <application
        android:allowBackup="true"
        android:icon="@drawable/goodgreenlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/goodgreenlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".KeyGenPage">

        </activity>

        <activity android:name=".UpdateInfoPage">

        </activity>

        <activity android:name=".LogInPage"> </activity>
    </application>


    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.VIBRATE"/>

</manifest>